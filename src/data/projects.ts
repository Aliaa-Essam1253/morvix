import type { Project } from './contentTypes';

// Add approved MORVIX case studies here. The Projects page automatically switches
// from the empty publication state to the case-study grid when this array has items.
export const projects: Project[] = [];
